# Evaluation fixtures

`dataset.json` contains 12 classification examples, `adversarial.json` contains
8 classification examples with conflicting instructions, and `extraction.json`
contains 5 refund-field examples. These are small synthetic regression fixtures.
Run `python -m scripts.evaluate_ai` from `backend/`.
